{
  "name": "the-operators-trading-journal-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Android and iOS wrapper for The Operators Trading Journal",
  "scripts": {
    "web:sync": "node scripts/sync-web.mjs",
    "cap:sync": "npm run web:sync && cap sync",
    "android:add": "npm run web:sync && cap add android",
    "ios:add": "npm run web:sync && cap add ios",
    "android:open": "npm run cap:sync && cap open android",
    "ios:open": "npm run cap:sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "8.4.3",
    "@capacitor/app": "8.0.1",
    "@capacitor/core": "8.4.3",
    "@capacitor/ios": "8.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.3"
  }
}
